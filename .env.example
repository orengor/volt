# Optional shared leaderboard (Upstash / Vercel KV).
# Without these, local file fallback works on a machine; Vercel uses in-memory.
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# or:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
